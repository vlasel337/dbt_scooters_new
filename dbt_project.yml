name: 'dbt_scooters_new'
version: '1.0.0'

profile: 'dbt_scooters_new'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_scooters_new:
    +materialized: table

version: 2

models:
  - name: "revenue_daily"
    description: "Daily revenue aggregate"
    config:
      materialized: "incremental"
      full_refresh: false
      strategy: "merge"
      unique_key: [date]
      on_schema_change: "append_new_columns"